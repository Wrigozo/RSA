package main.java;

import java.math.BigInteger;
import java.util.Random;

public class TheAlgorithm {
    private static Random r = new Random();
    private BigInteger BigRandomByBits(int bitSize) {
        int i=r.nextInt(10);
        BigInteger result = BigInteger.valueOf(0);

        return result;
    }
    }



